<% @cart.each do | product | %>
<p><%= product %></p>
<% end %>

<%= form_for root_path do |f| %>
<%= f.label :product %>
<%= f.text_field :product, id: "product" %>
<%= f.submit "add to cart"%>
<% end %>
